---
title: okfctl
description: A command-line tool for authoring and maintaining Open Knowledge Format (OKF) bundles — pure Go, no CGO, no model runtime.
template: splash
hero:
  tagline: Author and maintain Open Knowledge Format bundles from the command line. A conformant consumer of the OKF spec — pure Go, no CGO, no model runtime.
  actions:
    - text: Concepts
      link: /concepts/
      icon: right-arrow
      variant: primary
    - text: Command reference
      link: /reference/commands/
      icon: document
    - text: View on GitHub
      link: https://github.com/cwest/okfctl
      icon: external
      variant: minimal
---

import { Card, CardGrid } from "@astrojs/starlight/components";
import { fetchLatestVersion } from "../../lib/version.ts";

export const version = await fetchLatestVersion();

## Install

On macOS (or Linux with Homebrew), install with `brew`:

```sh
brew install cwest/tap/okfctl
```

On any macOS or Linux machine, install with the one-liner — it detects your
OS/arch, downloads the matching release archive, verifies its checksum, and puts
both `okfctl` and `okfctl-search` on your `PATH`:

```sh
curl -sSL https://okfctl.dev/install.sh | sh
```

If you have a Go toolchain, install from source:

<pre><code>{version.tag
  ? `go install github.com/cwest/okfctl@${version.tag}`
  : "go install github.com/cwest/okfctl@latest"}</code></pre>

{version.tag ? (
  <p>Latest release: <strong>{version.tag}</strong>.</p>
) : (
  <p>Installs the latest tagged release.</p>
)}

Or download a prebuilt binary for your platform from the
[releases page](https://github.com/cwest/okfctl/releases) — macOS, Linux, and
Windows, on amd64 and arm64. Each archive bundles both `okfctl` and the
`okfctl-search` plugin. Linux users can also grab a `.deb` or `.rpm`. Release
artifacts ship with an SBOM and are signed with cosign (keyless), verifiable
with `cosign verify-blob`.

## What it is

okfctl scaffolds a bundle, adds and moves nodes without breaking links, keeps
the reserved `index.md`/`log.md` files current, checks a corpus against the
spec, and reports on its health and link graph.

The [Open Knowledge Format](https://github.com/GoogleCloudPlatform/knowledge-catalog/blob/main/okf/SPEC.md)
is a specification **owned by Google** (`GoogleCloudPlatform/knowledge-catalog`).
okfctl does **not** author that spec — it is a **consumer that conforms to it**.
Where the spec defines behavior, the spec wins. The tool enforces the spec
*floor* for everyone and keeps anything stricter behind an explicit opt-in
overlay, so an unknown `type` or a future frontmatter key never fails `validate`.

<CardGrid stagger>
  <Card title="Concepts" icon="open-book">
    Bundles, nodes, the link graph, and the reserved files — read this first.
    [Concepts](/concepts/)
  </Card>
  <Card title="Author a bundle" icon="pencil">
    From an empty directory to a validated bundle.
    [Authoring guide](/guides/authoring/)
  </Card>
  <Card title="Curation health" icon="approve-check">
    `lint`, `analyze`, and `--strict` in CI.
    [Curation health](/guides/curation-health/)
  </Card>
  <Card title="Command reference" icon="list-format">
    Every command, its flags, and a runnable example.
    [Command reference](/reference/commands/)
  </Card>
</CardGrid>
